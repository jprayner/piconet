{
  "name": "monitor",
  "version": "1.0.0",
  "description": "Listens for traffic on an Econet network using the Piconet interface.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "James Rayner",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.18.12",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@jprayner/piconet-nodejs": "^1.0.1"
  }
}
